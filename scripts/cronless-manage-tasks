#!/bin/bash
# cronless-manage-tasks — управление задачами cronless (без шаблонов)

set -euo pipefail

UNIT_DIR="$HOME/.config/systemd/user"
TASKS_DIR="$HOME/.config/systemd/user/cronless-tasks"

mkdir -p "$UNIT_DIR" "$TASKS_DIR"

ACTION="${1:-}"

usage() {
  echo "Usage:"
  echo "  $0 add <task_name> \"<command>\" \"<OnCalendar>\""
  echo "  $0 remove <task_name>"
  echo "  $0 list"
  exit 1
}

is_one_line_bash_script() {
    local file="$1"

    # файл существует и исполняемый
    [[ -f "$file" && -x "$file" ]] || return 1

    # проверка shebang на bash
    head -n 1 "$file" | grep -Eq '^#!.*/(env +)?bash' || return 1

    # считаем строки, содержащие НЕ только пробелы/табы
    local lines
    lines=$(grep -cEv '^[[:space:]]*$' "$file")

    [[ "$lines" -eq 2 ]]
}


case "$ACTION" in
  add)
    [ $# -eq 4 ] || usage

    TASK_NAME="$2"
    TASK_COMMAND="$3"
    TASK_SCHEDULE="$4"

    SERVICE_FILE="$UNIT_DIR/${TASK_NAME}.service"
    TIMER_FILE="$UNIT_DIR/${TASK_NAME}.timer"
    TASK_FILE="$TASKS_DIR/${TASK_NAME}"

    if [ -e "$SERVICE_FILE" ] || [ -e "$TIMER_FILE" ] || [ -e "$TASK_FILE" ]; then
      echo "❌ Task '$TASK_NAME' already exists"
      exit 1
    fi

    # ---------- task config ----------
    cat > "$TASK_FILE" <<EOF
#!/usr/bin/env bash
$TASK_COMMAND
EOF
    chmod +x "$TASK_FILE"

    # ---------- service ----------
    cat > "$SERVICE_FILE" <<EOF
[Unit]
Description=Cronless task $TASK_NAME

[Service]
Type=oneshot
ExecStart=/usr/bin/cronless-run-task $TASK_NAME
EOF

    # ---------- timer ----------
    cat > "$TIMER_FILE" <<EOF
[Unit]
Description=Cronless timer $TASK_NAME

[Timer]
OnCalendar=$TASK_SCHEDULE
Persistent=true

[Install]
WantedBy=timers.target
EOF

    systemctl --user daemon-reload
    systemctl --user enable --now "$(basename "$TIMER_FILE")"

    echo "✅ Task '$TASK_NAME' added"
    echo "   Command : $TASK_COMMAND"
    echo "   Schedule: $TASK_SCHEDULE"
    ;;

  remove)
    [ $# -eq 2 ] || usage

    TASK_NAME="$2"

    systemctl --user disable --now "$TASK_NAME.timer" 2>/dev/null || true

    rm -f \
      "$UNIT_DIR/${TASK_NAME}.service" \
      "$UNIT_DIR/${TASK_NAME}.timer" \
      "$TASKS_DIR/${TASK_NAME}"

    systemctl --user daemon-reload
    systemctl --user reset-failed "$TASK_NAME.service"


    echo "🗑 Task '$TASK_NAME' removed"
    ;;

  list)
    echo "📋 Configured tasks:"
    shopt -s nullglob
    for TASK_FILE in "$TASKS_DIR"/*; do
      TASK_NAME="${TASK_FILE##*/}"

      CMD="$TASK_FILE"
      if is_one_line_bash_script "$TASK_FILE"; then
        line=$(sed -E '1{/^#!.*bash/d}; /^[[:space:]]*$/d' "$TASK_FILE")
        [[ $(wc -l <<<"$line") -eq 1 ]] && CMD=$line
      fi

      TIMER="$UNIT_DIR/$TASK_NAME.timer"

      if [ -f "$TIMER" ]; then
        SCHEDULE=$(grep '^OnCalendar=' "$TIMER" | cut -d= -f2-)
        STATUS=$(systemctl --user is-active "$TASK_NAME.timer" 2>/dev/null || echo inactive)
      else
        SCHEDULE="—"
        STATUS="missing timer"
      fi

      echo " - $TASK_NAME"
      echo "     command    : $CMD"
      echo "     schedule   : $SCHEDULE"
      echo "     status     : $STATUS"
    done
    ;;

  logs)
    journalctl --user -u $TASK_NAME.service
  *)
    usage
    ;;
esac
