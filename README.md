# Cronless

**Cronless** — лёгкий менеджер периодических задач на Linux с использованием systemd timers.  
Идеален для ноутбуков и рабочих станций, которые часто уходят в спящий режим. Задачи выполняются даже после пробуждения системы.

---

## 🚀 Возможности

- Лёгкая замена cron, без постоянной нагрузки на систему
- Добавление и удаление задач через CLI:
  - `cronless-manage-tasks add <name> "<command>" "<schedule>"`
  - `cronless-manage-tasks remove <name>`
  - `cronless-manage-tasks list`
- Логи и статусы:
  - stdout/stderr в `~/task_logs/logs/`
  - последний статус в `~/task_logs/status/`
- Еженедельный отчёт:
  - количество запусков
  - успешные и неудачные выполнения
  - максимальное время выполнения
  - последние 20 запусков
- Поддержка systemd timers с `Persistent=true` — задачи выполняются даже если ноут спал

---

## 📂 Структура проекта

## Работа без логина пользователя

Можно разрешить user-таймерам работать даже без активной сессии пользователя, включив linger:

```
loginctl enable-linger <username>
```

После этого systemd --user запускается при старте системы и продолжает работать, даже если пользователь не залогинен.
Это позволяет Cronless выполнять задачи по расписанию, даже если ноутбук включён, а пользователь не вошёл.

Проверка статуса:

```
loginctl show-user <username> | grep Linger
```

## Формат расписания

Systemd использует формат CalendarEvents. Несколько примеров:

|  Значение  |  Что делает  |
| :------------ | :------------ |
|  daily  | Каждый день в 00:00  |
|  weekly |  Каждый понедельник в 00:00 |
|  *-*-* 03:30:00 |	Каждый день в 03:30 |
|  Mon *-*-* 02:00:00	| Каждую неделю в понедельник 02:00 |
|  *:0/30	| Каждые 30 минут (каждый час) |
|  *-*-* *:0/15:00 | Каждые 15 минут (время в формате HH:MM:SS) |